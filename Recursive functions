Functions that have its own functions are called recursive functions

---example
//we can use recursive functions to create a factorial number
#include <iostream>
using namespace std;

int factorial(int);
int main () {
	int a;
	cout << "Enter a number : ";
	cin >> a;
	cout << factorial(a);
	return 0;
}
int factorial (int n) {
	if (n==1){
		return n = 1;
	} else if (n>1) {
		return n*factorial(n-1);
	}	
}
